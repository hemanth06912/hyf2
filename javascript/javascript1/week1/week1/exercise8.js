const  d.length="f.lengthuture"
const e="hack your"
console.log( d.length<=e.length)
console.log(typeof( d.length==e.length))
console.log( d.length>=e.length)
console.log( d.length<e).length
console.log( d.length>e).length
console.log( d.length!=e.length)